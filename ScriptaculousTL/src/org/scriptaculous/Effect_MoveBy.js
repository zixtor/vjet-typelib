/*
 * Only for backwards compatibility
 */
vjo.ftype('org.scriptaculous.Effect_MoveBy') //< public
//> needs(org.scriptaculous.Effect_Move)
.props({
	/**
	 * 
	 */
	//> public Effect_Move _invoke_({String | Element} idOrElement, Number topLeft, Number topRight)
	_invoke_: function() { return null ; }
})

.options({
	metatype: true
})
.endType();
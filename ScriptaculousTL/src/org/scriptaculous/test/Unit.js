vjo.ctype('org.scriptaculous.test.Unit') //< public

.props({
	//> public String inspect(Object obj)
	inspect: vjo.NEEDS_IMPL // same as Object.inspect
})
.protos({
	
})
.options({
	metatype: true
})
.endType();